# 后台管理功能测试报告

- **测试日期**：2026-09-10
- **测试对象**：游易达游戏担保交易平台（game-trade-h5）后台管理 `#/admin`
- **测试方式**：浏览器实际操作（线上隧道地址）+ 接口验证
- **测试账号**：管理员 13600000000 / Admin2026（运营主管）

---

## 一、五个标签页测试结果

| 标签页 | 结果 | 测试内容 |
|---|---|---|
| 用户资金 | ✅ 通过 | 按手机号/姓名搜索（无结果提示"没有匹配的用户"）；加款/扣款弹窗正常；空金额/错误金额校验"请输入正确金额"；余额不足拦截"扣款失败：该用户余额仅 ¥X"；成功提示显示最新余额；流水弹窗可查 admin_adjust 变动记录（含 ±金额和备注）。测试中对李娜账户的加扣款已全部冲回，余额恢复 **¥10732.00** |
| 卖家审核 | ✅ 通过 | 新建 2 个入驻申请验证：**审核通过**（测试卖家甲 → 已通过，待审数 2→1→0）；**驳回**（测试卖家乙 → 已驳回，弹窗填写驳回原因"资料不完整，缺少历史交易凭证，请补充后重新申请"并生效）；已通过/已封禁卖家不再显示审核按钮 |
| 商品管理 | ✅ 通过 | 新增（空表单拦截"请填写游戏名称和商品标题"；成功提示"新增成功"，后台列表与买家首页同步可见）→ 下架（提示"已下架"，买家端商品数 7→6 立即隐藏）→ 上架（"已上架"）→ 编辑（标题/价格更新，"已保存"）→ 删除（原生确认弹窗 → "已删除"）全流程正常；**删除保护验证通过**：有进行中订单的商品被拦截并提示"该商品有进行中的订单，不能删除，请改为下架" |
| 纠纷仲裁 | ✅ 通过 | 纠纷单完整显示：订单号、金额、发起人（买家/卖家）、发起时间、纠纷理由、发货状态、仲裁结果（如"买家胜诉，货款已退回"）及处理备注。当前 1 单为已结案历史单 |
| 客服 | ✅ 通过 | 会话列表显示用户数与未读红点；可打开会话查看完整聊天记录（双方消息+时间）；管理员发送回复成功，用户端可实时看到；返回列表后最后一条消息预览实时更新（3 秒轮询） |

---

## 二、测试中发现并修复的 3 个 Bug

### Bug 1（严重）：后台/列表商品卡片高度为 0，看不见也点不到
- **现象**：商品管理页卡片不可见，按钮点击无反应（实际点到了上方统计栏）。
- **根因**：`#admin-body{display:flex;flex-direction:column;overflow-y:auto}` 与 `.order-card{overflow:hidden}` 组合下，flex 子项默认最小高度自适应 + overflow:hidden 导致卡片被压缩到高度 0。买家端列表页同样受影响。
- **修复**：`public/css/style.css` 增加 `.view > *,#admin-body > *{flex-shrink:0}`。
- **验证**：修复后卡片高度恢复 139–153px，所有按钮可正常点击。

### Bug 2：价格显示双 ¥ 符号（¥¥9.9）
- **根因**：`.price::before{content:'¥'}` 会自动加 ¥，但 app.js 中有 3 处又硬编码了 `¥${...}`（余额卡片、加扣款弹窗、商品价格）。
- **修复**：删除 app.js 中 3 处重复的 ¥ 字面量。

### Bug 3："平台自营"标记随登录的管理员变化
- **现象**：同一商品在不同管理员账号下"平台自营"标记不一致。
- **根因**：`/api/admin/products` 用 `isPlatform: p.sellerId === me.id` 判断，把"是不是我发布的"错当成"是不是平台商品"。
- **修复**：server.js 改为 `isPlatform = p.platform === true || (卖家存在 && 卖家.role === 'admin')`；管理员后台发布的商品创建时即带 `platform: true` 标记。修复后已重启服务验证。

> 前端文件每次请求从磁盘读取，改前端无需重启；改 server.js 需重启 node 服务。静态资源版本号已升至 `?v=20260910b`。

---

## 三、测试数据说明

1. 用户资金测试中对买家"李娜"的加扣款（+88.50 / -50 / -38.50 冲回）已全部还原，余额 **¥10732.00** 无偏差。
2. 测试中曾误删商品 p5（梦幻西游 游戏币 1000万金币 ¥268，卖家张伟，状态已售）。删除保护逻辑本身正常——该商品无进行中订单（保护仅拦截 pending_pay/paid/delivered/disputed 状态订单），数据无法恢复，特此记录。
3. 乱码测试商品 p11（标题 `????-???????`，¥66.6）因有进行中订单**无法删除**，已执行**下架**处理，买家端不可见。
4. 为验证卖家审核流程新增 2 个测试账号（演示数据，可保留）：
   - `13611112222 / test1234`（测试卖家甲，**审核已通过**）
   - `13633334444 / test1234`（测试卖家乙，**已驳回**）
5. 客服会话中发送了一条测试回复："【后台测试】您好，客服回复功能测试正常，感谢反馈！"

---

## 四、结论

后台管理 5 个标签页（用户资金、卖家审核、商品管理、纠纷仲裁、客服）功能**全部测试通过**。测试过程中发现的 3 个 Bug（1 个严重样式问题、1 个显示问题、1 个标记逻辑问题）均已修复并验证。

⚠️ 若打开页面未见最新样式，请按 **Ctrl + F5** 强制刷新（最新静态资源版本 v=20260910b）。
